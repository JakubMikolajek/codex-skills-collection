# Roadmap

## [1.3.x]

- Add stronger examples for combined skills (frontend + backend + data in one flow).
- Extend `scripts/validate-routing-tree.sh` with stricter duplicate-ownership rules.
- Add more "ready to copy" templates in `skills/*/references/`.

## Ongoing

- Keep `AGENTS.md` and `skills/routing/*.md` aligned.
